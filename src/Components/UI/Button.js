import styled from "styled-components";

const Button = styled.button`
  font: inherit;
  cursor: pointer;
  border: none;
  padding: 0.5rem 2rem;
  border-radius: 25px;
  font-weight: bold;
`;

export default Button;

import styled from "styled-components";

const Button = styled.button`
  font: inherit;
  cursor: pointer;
  border: none;
  margin-right: 1rem;
  padding: 0.5rem 3rem;
  border-radius: 25px;
  font-weight: bold;
`;

export default Button;
